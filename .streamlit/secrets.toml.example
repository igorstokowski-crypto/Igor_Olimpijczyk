# Skopiuj jako .streamlit/secrets.toml i uzupełnij (NIE commituj do GitHub!)
# Potrzebne tylko jeśli hostujesz dashboard.py na Streamlit Cloud —
# na własnym VPS wystarczy zwykły plik .env (patrz .env.example).

DATABASE_URL = "postgresql://user:haslo@host:5432/igor_health"
